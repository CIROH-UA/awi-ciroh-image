name: Build and Push Comm Nextgen

on:
  push:
    branches: 
      - ngen-2i2c
    tags: [ 'v*' ]
  pull_request:
    branches: [ ngen-2i2c ]
  workflow_dispatch:  # Allows manual triggering
    inputs:
      tag:
        description: 'Custom tag to apply (optional)'
        required: false
        default: 'manual'

env:
  IMAGE_NAME: ngiab-2i2c
  QUAY_ORG: awiciroh

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Quay.io
      uses: docker/login-action@v3
      with:
        registry: quay.io
        username: ${{ secrets.QUAY_CIROH_USERNAME }}
        password: ${{ secrets.QUAY_CIROH_PASSWORD }}

    - name: Extract metadata (tags, labels) for Docker
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: quay.io/${{ env.QUAY_ORG }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=semver,pattern={{major}}
          type=sha
          ${{ github.event_name == 'workflow_dispatch' && format('type=raw,value={0}', inputs.tag) || '' }}
    - name: Free disk space
      run: |
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /opt/ghc
        docker system prune -af
    - name: Build and push
      uses: docker/build-push-action@v5
      with:
        context: .
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
